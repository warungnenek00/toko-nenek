{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "touch /tmp/database.sqlite && php artisan migrate --force && php artisan config:clear && php artisan cache:clear && php artisan serve --host=0.0.0.0 --port=$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
